exports.createRip = client => {
    return async (req, res) => {
        try {
            const body = req.body;
            if (!body) return res.status(400).send('Bad Request');

            

        } catch (err) {
        }
    }
}